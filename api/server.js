// Importing Modules
const express = require('express')

// Instance
const app = express()

const port = 3000

app.listen(port, ()=>{
    console.log("SERVER RUNNING ON PORT: 3000")
})

//create repo(form3), add collaborator, push to github
//research objects in javascript and ip address and port no.

/**
 * 
 */
